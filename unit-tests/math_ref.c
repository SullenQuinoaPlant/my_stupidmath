#include <math.h>

long double	lm_powl(long double x, long double y)
{
	return (powl(x, y));
}

long double	lm_expl(long double x)
{
	return (expl(x));
}

long double lm_logl(long double x)
{
	return (logl(x));
}
